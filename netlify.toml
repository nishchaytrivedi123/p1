[build]
  command = "apt-get update && apt-get install -y libpq-dev && pip install --upgrade pip && pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"

  [build.environment]
  PYTHON_VERSION = "3.9"  # Specify the Python version you want to use
